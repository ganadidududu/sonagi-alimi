import Foundation

/// Copy this file to `Secrets.swift` (already gitignored) and fill in your
/// own key. See `Secrets.swift`'s header comment for where to get one.
enum Secrets {
    static let kmaServiceKey = "YOUR_KMA_SERVICE_KEY"
}
